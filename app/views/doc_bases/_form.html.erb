<%= form_for(@doc_basis) do |f| %>
  <% if @doc_basis.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doc_basis.errors.count, "error") %> prohibited this doc_basis from being saved:</h2>

      <ul>
      <% @doc_basis.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :exclusive %><br />
    <%= f.check_box :exclusive %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
