<%= Touch::TimeLogs::Doing.netzke(self) do |config| config.content_hash do 
{
  :stop_path => end_activity_path(@activity.id,@time_log.id),
  :activity_name => @activity.name,
  :title => "LivingToLearn"
}
end end %>

