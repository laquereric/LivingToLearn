<% content_for :message_region do %>
  Time Log List for<br/>
  <%= render "activity" %>
<% end %>

<% content_for :target_region do %>
<table>

  <tr>
    <th>Id</th>
    <th>Start Time</th>
    <th>End Time</th>
  </tr>

<% @time_logs.each do |time_log| %>
  <tr>
    <td><%= time_log.id %></td>
    <td><%= time_log.start_time.strftime('%m/%d/%Y %I:%M %p') %> </td>
    <td><%= time_log.end_time.strftime('%m/%d/%Y %I:%M %p') %> </td>
    <td><%= link_to 'Show', activity_time_log_path(@activity.id, time_log.id) %></td>
    <td><%= link_to 'Edit', edit_activity_time_log_path(@activity.id, time_log.id) %></td>
    <td><%= link_to 'Destroy', activity_time_log_path(@activity.id, time_log.id), :confirm => 'Are you sure?', :method => :delete %></td>

  </tr>
<% end %>

</table>

<br />

<%= link_to 'New Time Log', new_activity_time_log_path(@activity) %> |
<%= link_to 'Show Activity', activity_path( @activity.id )  %>

<% end %>
